// Delete without head pointer - easy - 31/07/2023

class Solution
{
    void deleteNode(Node del)
    {
         Node temp = del.next;
         del.data = temp.data;
         del.next = temp.next;
         temp = null;
    }
}
